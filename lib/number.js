'use strict';

// FUNCTIONS //


// PDF //

/**
* FUNCTION: pdf( x, n, p )
*	Evaluates the probability density function (PDF) for a Binomial distribution with number of trails `n` and success probability `p` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} n - number of trails
* @param {Number} p - success probability
* @returns {Number} evaluated PDF
*/
function pdf( x, n, p ) {

} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;
